import { IsVariantPipe } from './is-variant-pipe';

describe('IsVariantPipe', () => {
  it('create an instance', () => {
    const pipe = new IsVariantPipe();
    expect(pipe).toBeTruthy();
  });
});
